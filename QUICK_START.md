# 🚀 دليل التشغيل السريع - Volo Platform

## متطلبات النظام
- Node.js 18+
- PostgreSQL 14+
- npm أو yarn

## خطوات التشغيل

### 1. تثبيت المتطلبات
```bash
npm install
```

### 2. إعداد قاعدة البيانات
```bash
# إنشاء قاعدة البيانات (إذا لم تكن موجودة)
createdb volo

# إنشاء الجداول
npx prisma db push

# إضافة البيانات التجريبية
npm run seed
```

### 3. تشغيل المشروع
```bash
npm run dev
```

### 4. فتح المتصفح
```
http://localhost:3000
```

## 🧪 حسابات التجريب

### المدير
- **البريد**: admin@volo.dz
- **كلمة المرور**: admin123

### صاحب فندق
- **البريد**: owner@volo.dz
- **كلمة المرور**: owner123

### مستخدم عادي
- **البريد**: user@volo.dz
- **كلمة المرور**: user123

## 📱 الصفحات المهمة

- **الرئيسية**: http://localhost:3000
- **البحث**: http://localhost:3000/search
- **تسجيل الدخول**: http://localhost:3000/login
- **إنشاء حساب**: http://localhost:3000/register
- **لوحة تحكم المستخدم**: http://localhost:3000/dashboard
- **لوحة تحكم المالك**: http://localhost:3000/owner/overview

## 🔧 أوامر مفيدة

```bash
# فحص الأخطاء
npm run lint

# إنشاء build
npm run build

# تشغيل الإنتاج
npm start

# إعادة إنشاء قاعدة البيانات
npx prisma db push --force-reset

# عرض البيانات في Prisma Studio
npx prisma studio
```

## ❗ نصائح للتطوير

1. **قاعدة البيانات**: تأكد من تشغيل PostgreSQL
2. **متغيرات البيئة**: ملف .env محدث للاستخدام المحلي
3. **البيانات التجريبية**: استخدم `npm run seed` لإضافة بيانات تجريبية
4. **SATIM**: يتم استخدام الوضع التجريبي للدفع

## 🆘 حل المشاكل الشائعة

### خطأ في الاتصال بقاعدة البيانات
```bash
# تأكد من تشغيل PostgreSQL
sudo service postgresql start

# أو استخدام Docker
docker run --name postgres-volo -e POSTGRES_PASSWORD=admin123 -e POSTGRES_DB=volo -p 5432:5432 -d postgres:14
```

### خطأ في التبعيات
```bash
# حذف node_modules وإعادة التثبيت
rm -rf node_modules package-lock.json
npm install
```

### خطأ في Prisma
```bash
# إعادة إنشاء client
npx prisma generate

# إعادة إنشاء الجداول
npx prisma db push --force-reset
npm run seed
```

---

## 🎉 مبروك! منصة Volo جاهزة!

تم إنشاء منصة حجز الفنادق بنجاح! 🎉

### الميزات المتاحة:
- ✅ بحث عن الفنادق
- ✅ تسجيل الدخول وإنشاء الحسابات
- ✅ صفحات تفاصيل الفنادق
- ✅ نظام الحجوزات
- ✅ لوحات التحكم
- ✅ نظام الدفع التجريبي
- ✅ واجهة عربية متجاوبة

**استمتع بتطوير المشروع!** 🚀